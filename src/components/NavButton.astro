---
export interface Props {
  href: string;
}

const { href } = Astro.props as Props;
---

<a
  href={href}
  class="block w-fit rounded-lg border-2 border-red-100 px-4 py-2 font-bold transition ease-out hover:-translate-y-[2px] hover:bg-red-100 hover:text-white md:inline"
>
  <slot />
</a>
